{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .trustreviews-{{ section.id }} .trustreviews__header {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    padding: 0 0 1.75rem;
    margin: 0 0 2.5rem;
  }

  .trustreviews-{{ section.id }} .trustreviews__header-title {
    font-weight: 800;
    font-size: 2.8rem;
    margin: 0;
    color: #191919;
  }

  .trustreviews-{{ section.id }} .trustreviews__score {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    flex-wrap: wrap;
  }

  .trustreviews-{{ section.id }} .trustreviews__score-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: #007f4e;
    color: #ffffff;
    font-weight: 800;
    font-size: 1.1rem;
    padding: 0.5rem 0.9rem;
    border-radius: 4px;
    margin: 0;
  }

  .trustreviews-{{ section.id }} .trustreviews__score-badge-row {
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }

  .trustreviews-{{ section.id }} .trustreviews__score-number {
    font-size: 3.4rem;
    font-weight: 800;
    line-height: 1;
    color: #191919;
  }

  .trustreviews-{{ section.id }} .trustreviews__score-count {
    margin: 0.5rem 0 0;
    font-size: 1rem;
    color: rgba(0, 0, 0, 0.6);
  }

  .trustreviews-{{ section.id }} .trustreviews__stars-row {
    display: inline-flex;
    gap: 0.25rem;
  }

  .trustreviews-{{ section.id }} .trustreviews__star-box {
    width: 1.6rem;
    height: 1.6rem;
    background: #dcdce6;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 3px;
    flex: 0 0 auto;
  }

  .trustreviews-{{ section.id }} .trustreviews__star-box.is-filled {
    background: #007f4e;
  }

  .trustreviews-{{ section.id }} .trustreviews__star-box svg {
    width: 1.05rem;
    height: 1.05rem;
    fill: #ffffff;
  }

  .trustreviews-{{ section.id }} .trustreviews__star-box--lg {
    width: 2.3rem;
    height: 2.3rem;
    border-radius: 4px;
  }

  .trustreviews-{{ section.id }} .trustreviews__star-box--lg svg {
    width: 1.5rem;
    height: 1.5rem;
  }

  .trustreviews-{{ section.id }} .trustreviews__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }

  @media screen and (min-width: 750px) {
    .trustreviews-{{ section.id }} .trustreviews__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media screen and (min-width: 990px) {
    .trustreviews-{{ section.id }} .trustreviews__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .trustreviews-{{ section.id }} .trustreviews__card {
    border: 1px solid rgba(0, 0, 0, 0.08);
    border-radius: 8px;
    box-shadow: 0 1px 6px rgba(0, 0, 0, 0.05);
    padding: 1.85rem;
    display: flex;
    flex-direction: column;
    background: #ffffff;
    opacity: 1;
    transform: translateY(0);
    transition: opacity 0.35s ease, transform 0.35s ease;
  }

  .trustreviews-{{ section.id }} .trustreviews__card--hidden {
    display: none;
  }

  .trustreviews-{{ section.id }} .trustreviews__card--enter {
    opacity: 0;
    transform: translateY(14px);
  }

  .trustreviews-{{ section.id }} .trustreviews__card-top {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .trustreviews-{{ section.id }} .trustreviews__avatar {
    width: 3.8rem;
    height: 3.8rem;
    border-radius: 50%;
    background: #e7f4ee;
    color: #007f4e;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    font-size: 1.35rem;
    flex: 0 0 auto;
  }

  .trustreviews-{{ section.id }} .trustreviews__identity {
    min-width: 0;
  }

  .trustreviews-{{ section.id }} .trustreviews__author {
    font-weight: 700;
    font-size: 1.1rem;
    margin: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .trustreviews-{{ section.id }} .trustreviews__verified {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    margin: 0.2rem 0 0;
    font-size: 0.9rem;
    font-weight: 600;
    color: rgba(var(--color-foreground), 0.6);
  }

  .trustreviews-{{ section.id }} .trustreviews__verified svg {
    width: 1.25rem;
    height: 1.25rem;
    fill: #007f4e;
    flex: 0 0 auto;
  }

  .trustreviews-{{ section.id }} .trustreviews__stars {
    margin: 0 0 0.85rem;
  }

  .trustreviews-{{ section.id }} .trustreviews__title {
    font-weight: 800;
    font-size: 1.2rem;
    margin: 0 0 0.6rem;
  }

  .trustreviews-{{ section.id }} .trustreviews__quote {
    font-size: 1.05rem;
    line-height: 1.55;
    margin: 0 0 1.1rem;
    color: rgba(var(--color-foreground), 0.85);
    flex-grow: 1;
  }

  .trustreviews-{{ section.id }} .trustreviews__photo {
    width: 100%;
    aspect-ratio: 4 / 3;
    border-radius: 8px;
    overflow: hidden;
    margin: 0 0 1rem;
    background: rgba(var(--color-foreground), 0.05);
  }

  .trustreviews-{{ section.id }} .trustreviews__photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .trustreviews-{{ section.id }} .trustreviews__photo-placeholder {
    width: 100%;
    aspect-ratio: 4 / 3;
    border: 1px dashed rgba(var(--color-foreground), 0.25);
    border-radius: 8px;
    margin: 0 0 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
    color: rgba(var(--color-foreground), 0.4);
    text-align: center;
    padding: 0.5rem;
  }

  .trustreviews-{{ section.id }} .trustreviews__date {
    font-size: 0.9rem;
    color: rgba(var(--color-foreground), 0.5);
    margin: 0;
  }

  .trustreviews-{{ section.id }} .trustreviews__footer {
    text-align: center;
    margin-top: 2.5rem;
  }

  .trustreviews-{{ section.id }} .trustreviews__load-more {
    cursor: pointer;
    font-size: 1.05rem;
    padding: 1rem 2rem;
  }

  .trustreviews-outer-{{ section.id }} {
    background: #ffffff;
  }
{%- endstyle -%}

<div class="trustreviews-outer-{{ section.id }}">
  <div class="page-width section-{{ section.id }}-padding trustreviews trustreviews-{{ section.id }}" data-trustreviews-batch="{{ section.settings.reviews_per_page }}">
    <div class="trustreviews__header">
      <p class="trustreviews__header-title">{{ section.settings.heading }}</p>
      <div class="trustreviews__score">
        <p class="trustreviews__score-number">{{ section.settings.average_rating }}</p>
        <div>
          {%- assign rating_num = section.settings.average_rating | times: 1.0 -%}
          <div class="trustreviews__score-badge-row">
            <span class="trustreviews__score-badge">
              {%- if rating_num >= 4.5 -%}Excellent
              {%- elsif rating_num >= 4.0 -%}Great
              {%- elsif rating_num >= 3.0 -%}Average
              {%- else -%}Reviews
              {%- endif -%}
            </span>
            <div class="trustreviews__stars-row" aria-hidden="true">
              {%- assign full_stars = rating_num | round -%}
              {%- for i in (1..5) -%}
                <span class="trustreviews__star-box trustreviews__star-box--lg{% if i <= full_stars %} is-filled{% endif %}">
                  <svg viewBox="0 0 24 24"><path d="M12 2l2.9 6.6 7.1.6-5.4 4.7 1.6 7-6.2-3.8L6 21l1.6-7L2.2 9.2l7.1-.6z"/></svg>
                </span>
              {%- endfor -%}
            </div>
          </div>
          <p class="trustreviews__score-count">{{ section.settings.review_count }}</p>
        </div>
      </div>
    </div>

    <div class="trustreviews__grid">
      {%- assign visible_count = section.settings.reviews_per_page -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'review' -%}
          <div
            class="trustreviews__card{% if forloop.index > visible_count %} trustreviews__card--hidden{% endif %}"
            {{ block.shopify_attributes }}
          >
            {%- if block.settings.photo != blank -%}
              <div class="trustreviews__photo">
                <img
                  src="{{ block.settings.photo | image_url: width: 500 }}"
                  alt="{{ block.settings.photo.alt | escape }}"
                  loading="lazy"
                  width="{{ block.settings.photo.width }}"
                  height="{{ block.settings.photo.height }}"
                >
              </div>
            {%- elsif block.settings.show_photo_placeholder -%}
              <div class="trustreviews__photo-placeholder">[PLACEHOLDER — customer photo]</div>
            {%- endif -%}

            <div class="trustreviews__card-top">
              <div class="trustreviews__avatar" aria-hidden="true">{{ block.settings.author | slice: 0 }}</div>
              <div class="trustreviews__identity">
                <p class="trustreviews__author">{{ block.settings.author | default: '[Customer name]' }}</p>
                {%- if block.settings.verified -%}
                  <p class="trustreviews__verified">
                    <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M9 16.2 4.8 12l-1.4 1.4L9 19 21 7l-1.4-1.4z"/></svg>
                    Verified purchase
                  </p>
                {%- endif -%}
              </div>
            </div>

            <div class="trustreviews__stars trustreviews__stars-row" aria-hidden="true">
              {%- assign stars = block.settings.rating | times: 1 -%}
              {%- for i in (1..5) -%}
                <span class="trustreviews__star-box{% if i <= stars %} is-filled{% endif %}">
                  <svg viewBox="0 0 24 24"><path d="M12 2l2.9 6.6 7.1.6-5.4 4.7 1.6 7-6.2-3.8L6 21l1.6-7L2.2 9.2l7.1-.6z"/></svg>
                </span>
              {%- endfor -%}
            </div>

            {%- if block.settings.title != blank -%}
              <p class="trustreviews__title">{{ block.settings.title }}</p>
            {%- endif -%}

            <p class="trustreviews__quote">{{ block.settings.quote | default: '[PLACEHOLDER — real customer review]' }}</p>
            <p class="trustreviews__date">{{ block.settings.date }}</p>
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>

    <div class="trustreviews__footer">
      <button type="button" class="button button--secondary trustreviews__load-more">
        {{ section.settings.load_more_label }}
      </button>
    </div>
  </div>
</div>

<script>
  (function () {
    var section = document.currentScript.closest('.trustreviews-{{ section.id }}') || document.querySelector('.trustreviews-{{ section.id }}');
    if (!section) return;
    var batchSize = parseInt(section.getAttribute('data-trustreviews-batch'), 10) || 6;
    var button = section.querySelector('.trustreviews__load-more');
    if (!button) return;

    function revealNextBatch() {
      var hiddenCards = section.querySelectorAll('.trustreviews__card--hidden');
      if (!hiddenCards.length) {
        button.hidden = true;
        return;
      }
      var toReveal = Array.prototype.slice.call(hiddenCards, 0, batchSize);
      toReveal.forEach(function (card, index) {
        card.classList.remove('trustreviews__card--hidden');
        card.classList.add('trustreviews__card--enter');
        setTimeout(function () {
          card.classList.remove('trustreviews__card--enter');
        }, 20 + index * 60);
      });
      if (section.querySelectorAll('.trustreviews__card--hidden').length === 0) {
        button.hidden = true;
      }
    }

    if (section.querySelectorAll('.trustreviews__card--hidden').length === 0) {
      button.hidden = true;
    }

    button.addEventListener('click', revealNextBatch);
  })();
</script>

{% schema %}
{
  "name": "Trust reviews list",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Title",
      "default": "Customer reviews"
    },
    {
      "type": "inline_richtext",
      "id": "subheading",
      "label": "Subtitle",
      "default": "Real reviews from real Overtake customers"
    },
    {
      "type": "text",
      "id": "average_rating",
      "label": "Average rating",
      "default": "4.8"
    },
    {
      "type": "text",
      "id": "review_count",
      "label": "Review count text",
      "default": "Based on 12,000+ reviews"
    },
    {
      "type": "range",
      "id": "reviews_per_page",
      "min": 3,
      "max": 12,
      "step": 3,
      "label": "Reviews shown per page/click",
      "default": 6
    },
    {
      "type": "text",
      "id": "load_more_label",
      "label": "Load more button text",
      "default": "Load more reviews"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "review",
      "name": "Review",
      "settings": [
        {
          "type": "image_picker",
          "id": "photo",
          "label": "Customer photo (optional)"
        },
        {
          "type": "checkbox",
          "id": "show_photo_placeholder",
          "label": "Show empty photo placeholder if no photo is set",
          "default": false
        },
        {
          "type": "select",
          "id": "rating",
          "label": "Stars",
          "options": [
            { "value": "5", "label": "5" },
            { "value": "4", "label": "4" },
            { "value": "3", "label": "3" }
          ],
          "default": "5"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Review title",
          "default": "[PLACEHOLDER — short title]"
        },
        {
          "type": "textarea",
          "id": "quote",
          "label": "Review text",
          "default": "[PLACEHOLDER — real customer review]"
        },
        {
          "type": "text",
          "id": "author",
          "label": "Customer name",
          "default": "[Customer name]"
        },
        {
          "type": "text",
          "id": "date",
          "label": "Date text",
          "default": "[PLACEHOLDER — e.g. 2 weeks ago]"
        },
        {
          "type": "checkbox",
          "id": "verified",
          "label": "Show \"Verified purchase\" badge",
          "default": true
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Trust reviews list",
      "blocks": [
        { "type": "review" },
        { "type": "review" },
        { "type": "review" },
        { "type": "review" },
        { "type": "review" },
        { "type": "review" }
      ]
    }
  ]
}
{% endschema %}
